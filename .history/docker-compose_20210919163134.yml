version: '1'

services:
  fastapi:
    ports:
      - 2003:2003
    build:
      context: ./
      dockerfile: Dockerfile
    restart: always
    volumes:
      - ./api:/api
    command: >
      gunicorn --bind "127.0.0.1:2003" --log-level debug  main:app