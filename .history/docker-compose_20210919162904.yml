version: '1'

services:
  fastapi:
    ports:
      - 2003:2003
    build:
      context: ./
      dockerfile: Dockerfile
    restart: always
    volumes:
      - ./api:/api
      - /api/
    command: >
      ash -c "yarn install &&
      yarn dev"